#include <string>
#include <vector>
#include <queue>

using namespace std;

class Comparator {
public:
    bool operator() (vector<int> v1, vector<int> v2) {
        return v1[1] < v2[2];
    }
};

int solution(vector<vector<int>> jobs) {
    priority_queue<vector<int>, vector<vector<int>>, Comparator> pq(jobs.begin(), jobs.end());
    int start_time = 0;
    while (!pq.empty()) {
        
    }
}
