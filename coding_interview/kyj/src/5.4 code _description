5.4 다음 코드가 하는 일을 설명하라.
(( n & (n - 1)) == 0)

1. A & B == 0 // A와 B의 동시에 1인 비트가 없어야 한다.
2. n = abcde10000 인 경우, n-1 = abcde01111 (성립) // n은 2의 거듭 제곱이다.
   n = abcde10001 인 경우, n-1 = abcde10000 (성립X)

   따라서 다음 코드가 하는 일은 n이 2의 거듭제곱 수이거나 0임을 검사한다.